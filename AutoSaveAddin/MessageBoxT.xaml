<Window x:Class="AutoSaveAddin.MessageBoxT"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="185"
        Width="360"
        Name="uc"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F7FAF8"
        Loaded="MessageBoxT_OnLoaded"
        FocusManager.FocusedElement="{Binding ElementName=YesButton}">
    <Border Margin="12" Padding="16" Background="#FFFFFF" BorderBrush="#D7E2DE" BorderThickness="1" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="46" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="14" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.ColumnSpan="3" VerticalAlignment="Center">
                <TextBlock Text="{Binding Message}" FontSize="15" FontWeight="SemiBold" Foreground="#21312D" TextWrapping="Wrap" HorizontalAlignment="Center" />
                <TextBlock Text="Автосохранение продолжится автоматически после таймера" Margin="0,8,0,0" Foreground="#667871" HorizontalAlignment="Center" />
            </StackPanel>

            <Button Name="YesButton" Grid.Row="1" Grid.Column="0" Height="34" Command="{Binding YesCommand, ElementName=uc}" Background="#DFF2E6" BorderBrush="#8CC8A0" Foreground="#205B34" FontWeight="SemiBold">
                <TextBlock Text="{Binding Time, StringFormat=Да ({0})}" />
            </Button>
            <Button Name="NoButton" Grid.Row="1" Grid.Column="2" Height="34" Content="Нет" Command="{Binding NoCommand, ElementName=uc}" Background="#F7E1E1" BorderBrush="#D69A9A" Foreground="#7A2D2D" FontWeight="SemiBold" />
        </Grid>
    </Border>
</Window>
